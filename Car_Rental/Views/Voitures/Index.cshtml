@model IEnumerable<Car_Rental.Models.Voiture>

@{
    ViewBag.Title = "Index";
}

<section class="container-fluid text-center my-4">




    <!--Navbar-->
    @*<nav class="navbar navbar-expand-lg navbar-light bg-light mt-3 mb-5">*@

        <!-- Navbar brand -->
        @*<span class="navbar-brand">Categories:</span>*@

        <!-- Collapsible content -->
        <!-- Links -->
        @*<ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href="">All</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="">test</a>
            </li>


        </ul>*@
        <!-- Links -->

        @*@using (Html.BeginForm("Index", "Voitures", FormMethod.Get))
        {
            <div class="form-inline mr-auto">
                <div class="md-form my-0">
                    <input class="form-control mr-sm-2" id="search-input" name="carburant" type="text" placeholder="Rechercher par type de carburant" value="@Request["carburant"]">
                    <input type="submit" value="Rechercher" id="search-btn" class="btn btn-secondary my-2 my-sm-0">
                </div>
            </div>
        }*@

        <!-- Collapsible content -->

    @*</nav>*@
    <!--/.Navbar-->
    <!--Grid row-->
    <div class="row">
        <!--Grid column-->
        @foreach (var item in Model)
        {

            <div class="col-md-4 col-sm-6 mb-4">

                <!--Card-->
                <div class="card h-100 bg-light">

                    <!--Card image-->
                    <div class="card-img-top">
                        <img src="@Url.Content("~/Content/Images/"+@item.Image)" class="card-img-top" alt="@Html.DisplayFor(modelItem => item.Modèle.Serie)">
                    </div>
                    <!--Card image-->
                    <!--Card content-->
                    <div class="card-body text-center">
                        <!--Category & Title-->
                        <div class="">
                            <h6>
                                Catégorie :
                                <span class="dark-grey-text">@Html.DisplayFor(modelItem => item.Catégorie.Nom)</span>
                            </h6>
                            <h6>
                                Marque :
                                <span class="dark-grey-text">@Html.DisplayFor(modelItem => item.Modèle.Marque) @Html.DisplayFor(modelItem => item.Modèle.Serie)</span>
                            </h6>
                            <h5 style="color:gray">
                                @Html.DisplayFor(modelItem => item.Prix_location) MAD / Jours
                            </h5>
                        </div>
                        <div class="d-flex justify-content-between">
                            @Html.ActionLink("+ de details", "Details", new { id = item.Id_voiture }, new { @class = "btn btn-light btn-block btn-voiture" })
                        </div>
                    </div>
                    <!--Card content-->
                </div>
                <!--Card-->
            </div>
            <!--Grid column-->
        }
    </div>
</section>
